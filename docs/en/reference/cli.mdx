---
title: CLI Reference
description: luxc commands for lexing, parsing, linting, formatting, compiling, LSP, GMod builds, error mapping, and GMA packaging.
---

# CLI Reference

The CLI reference assumes the prebuilt `luxc` is available on `PATH`. Download
the release archive and run commands as:

```powershell
luxc <command>
```

If `luxc` is not on `PATH`, use the explicit executable path:

```powershell
.\tools\luxc\luxc.exe <command>
```

Compiler contributors can run the same commands from source, but normal Lux
projects should use the prebuilt `luxc`. Official packages such as `@lux/std`
are installed per project with `luxc init --std` or `luxc install`.

## Command Overview

```text
luxc lex <path>
luxc parse <path>
luxc lint <path>
luxc format <path> [--check] [--write]
luxc init [path] [--name <name>] [--std] [--out <path>] [--runtime-base <path>] [--no-autorun]
luxc install <package-id> --from <github:owner/repo|url|path> [--tag <tag>|--branch <branch>|--commit <commit>]
luxc remove <package-id> [--project <project-root>]
luxc lock [project-root]
luxc list [project-root]
luxc doctor [project-root]
luxc lsp
luxc compile <path> [--map <path>] [--source-comments none|readable|boundary|dense]
luxc map-error <map.json> <generated-line>
luxc gmod build <source-root> --out <path> [--runtime-base <path>] [--no-autorun] [--dry-run]
luxc gmod build --manifest <lux.toml> [--out <path>] [--runtime-base <path>] [--no-autorun] [--dry-run]
luxc gmod package --manifest <lux.toml> --root <path> --gmad <path> --out <path> [--run] [--build-out <path>] [--runtime-base <path>] [--no-autorun]
```

All paths are read and written as UTF-8 text.

## `lex`

Print tokens for one file:

```powershell
luxc lex examples\features.lux
```

Use this only when debugging parser or lexer behavior. Normal users should not
need it.

## `parse`

Parse one file and print the syntax tree:

```powershell
luxc parse examples\features.lux
```

This is useful when checking whether a new syntax form is recognized before
lowering or codegen is implemented.

## `lint`

Run syntax, resolver, module, and style checks without generating Lua:

```powershell
luxc lint examples\features.lux
```

`lint` reports diagnostics with source spans. It is the cheapest command to run
in editors and pre-commit checks.

<LuxLuaPair title="Disable one lint locally" luxLabel="Lux" luaLabel="Check meaning">

```lux
@lint.disable("unused-local")
local debugValue = expensiveProbe()
```

```text
The disable applies to the annotated declaration.
It does not change generated Lua semantics.
```

</LuxLuaPair>

Use local disables sparingly. If a rule is wrong for a whole project, configure
the rule rather than scattering suppressions.

## `format`

Check formatting:

```powershell
luxc format src\inventory\module.lux --check
```

Write formatting changes:

```powershell
luxc format src\inventory\module.lux --write
```

Formatter output is intended to preserve comments and keep Lux-specific syntax
readable. Generated Lua formatting is controlled by the emitter, not this
command.

## `init`

Create a project skeleton:

```powershell
luxc init my_addon
```

Plain `init` is offline and writes an empty `[dependencies]` table. Add
`--std` when the project should install the official standard package set:

```powershell
luxc init my_addon --std
```

By default, `init` creates an addon-friendly GMod configuration:

```toml
[target.gmod]
source_root = "src"
out = "generated/lua"
runtime_base = "lux/my_addon"
autorun = true
```

Use `--no-autorun` when an existing entry point should include the generated
Lux loaders:

```powershell
luxc init my_addon --no-autorun --out generated
```

## `install`

Add a direct package dependency and rewrite `lux.lock`:

```powershell
luxc install @lux/gmod --from github:TimeWatcher/lux-packages --project my_addon
```

Lux has no registry. The source passed with `--from` selects the package set:
`github:owner/repo`, a zip `url`, or a local `path`. For GitHub sources, pin the
selected package set with one of `--tag`, `--branch`, or `--commit`.

## `remove`

Remove a direct dependency from `lux.toml` and rewrite `lux.lock`:

```powershell
luxc remove @lux/gmod --project my_addon
```

Transitive packages disappear naturally when no remaining direct dependency
requires them. Removing a transitive-only package is rejected because it is not
owned by the project manifest.

## `lock`

Regenerate `lux.lock` from the current `lux.toml`:

```powershell
luxc lock my_addon
```

This command does not search for newer versions. With Lux's registryless model,
version choice is the explicit source ref in the manifest; package versions in
`lux.package.toml` are compatibility checks for the selected package graph.

## `list`

Print locked packages:

```powershell
luxc list my_addon
```

## `doctor`

Inspect package manager state:

```powershell
luxc doctor my_addon
```

## `lsp`

Start the compiler-backed Language Server Protocol process:

```powershell
luxc lsp
```

Editors should launch this command from the compiler selected by the project or
user configuration. This keeps diagnostics, completions, package resolution,
and builds on the same `luxc` version.

## `compile`

Compile one Lux file to Lua:

```powershell
luxc compile examples\features.lux
```

Emit source comments:

```powershell
luxc compile examples\features.lux --source-comments readable
```

Emit a sidecar source map:

```powershell
luxc compile examples\features.lux --map examples\features.lua.map.json
```

`compile` is for isolated examples and backend debugging. Use `gmod build` for
real addon projects so modules, packages, and realms are handled together.

## `map-error`

Map a generated Lua location back to Lux source:

```powershell
luxc map-error generated\features.lua.map.json 42
```

The input line/column should come from a GMod Lua stack trace or Lua syntax
error. When source comments are disabled in production output, sidecar maps are
the main way to recover Lux source locations.

## `gmod build`

Build a GMod project from a manifest:

```powershell
luxc gmod build --manifest lux.toml
```

Build with explicit paths:

```powershell
luxc gmod build src --out generated/lua
```

Preview the output plan without writing files:

```powershell
luxc gmod build --manifest lux.toml --dry-run
```

`gmod build` performs module discovery, package resolution, realm splitting,
loader generation, source map generation, and diagnostics for the whole project.
`--no-autorun` disables only the addon-style forwarder under `out/autorun`; the
Lux loader tree and module registry are still emitted.

## `gmod package`

Package a built addon with `gmad.exe`:

```powershell
luxc gmod package --manifest lux.toml --root generated --gmad C:\path\to\gmad.exe --out dist\my_addon.gma
```

Add `--run` to actually invoke `gmad`. Without `--run`, Lux prints the package
plan so CI and release scripts can inspect it.

```powershell
luxc gmod package --manifest lux.toml --root generated --gmad C:\path\to\gmad.exe --out dist\my_addon.gma --run
```

Packaging does not replace `gmod build`; it packages the addon layout produced
by the build step. `--root` is the package root passed to `gmad`; it is separate
from the build `out` path. Use `--build-out` only when the package command
should temporarily override the manifest build output.
